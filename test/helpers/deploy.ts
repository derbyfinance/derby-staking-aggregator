// import { deployContract } from 'ethereum-waffle';
// import { Signer, BigNumberish } from 'ethers';

// import BasketToken from '../../artifacts/contracts/BasketToken.sol/BasketToken.json';
// import { BasketToken } from '@typechain/BasketToken';

// export const deployBasketToken = (deployerSign: Signer, ETFgame: Address, name: String, symbol: String): Promise<BasketToken> => {
//     return (deployContract(deployerSign, BasketTokenArtifact, [ETFgame, name, symbol])) as Promise<BasketToken>;
